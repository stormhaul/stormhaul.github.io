"use strict";

function init() {
    let grid = new Grid();
    let camera = new Camera();
    let renderer = new Renderer();
}

document.addEventListener("DOMContentLoaded", function() {
    init();
});